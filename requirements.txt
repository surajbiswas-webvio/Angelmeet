# Core test runner and Playwright integration.
pytest>=8.0,<9
pytest-playwright>=0.7,<1

# Pytest plugins: base URL injection and per-test timeouts.
pytest-base-url>=2.0,<3
pytest-timeout>=2.3

# Environment configuration and reporting.
python-dotenv>=1.0,<2
allure-pytest>=2.13